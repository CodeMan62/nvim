require("mason").setup({

    -- UI customizations
    ui = {
        border = "rounded", -- Use rounded borders for the Mason window
        icons = {
            package_installed = "✅",
            package_pending = "⏳",
            package_uninstalled = "❌"
        }
    },

})

